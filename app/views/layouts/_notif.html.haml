- flash.each do |flash_type, message|
  - bg, text = notif_color(flash_type)
  .absolute.top-8.right-10.animate-pulse{ class: bg, data: {controller: "notif", action: "animationiteration->notif#remove" }}
    .w-80.h-14
      %p.mx-5.my-2{class: text}
        = message
